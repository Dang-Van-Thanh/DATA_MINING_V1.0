# DATA_MINING_V1.0
```
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python scripts/run_pipeline.py
streamlit run app.py
```
❗❗❗ Nếu chưa cài xgboost, pipeline sẽ tự bỏ qua XGB và vẫn chạy bình thường. ❗❗❗
